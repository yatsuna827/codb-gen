# 引き継ぎメモ: とにかくバトルDB(FullDB / LightDB)生成のRust移植

## 何をしていたか

ポケモンコロシアム「とにかくバトル(シングル最強)」による現在seed特定用データベースの生成コードを Rust で書き直した(本リポジトリ)。
旧生成コードはC++(CODataBase.cpp)と C#(CODB_test、参照ライブラリ PokemonGCRNGLibrary はソース消失)。

前段の調査・設計の成果物は以下にある。

- `docs/design-compressed-lightdb.md`
  - LightDB圧縮フォーマット(単一ファイル・約450MB)の設計・見積
- `docs/alternative-tmto.md`
  - 不採用代替案の記録

※圧縮フォーマットの実装は未着手。本ツールは**現行(576ファイル)形式**を出力する

## 関連リポジトリ・データの所在

| 対象 | 場所 |
|---|---|
| 検索側C#(正のキー定義) | `PokemonCOSeedDataBaseAPI`(SeedSearcher.cs, BattleTeam.cs) |
| 生成ロジックの正(C#) | `PokemonCoRNGLibrary`(RentalTeamRank / GCSlot) |
| 旧C++生成器(参考・誤りあり) | `CODatabase/CODataBase.cpp` |
| 旧C#生成パイプライン(参考) | `CODB_test` |
| 現物LightDB(基準データ) | 576ファイル、計1,024,761,320B、128,095,165エントリ |

## DBの仕様(会話で確定した事実)

観測1回 = COMトレーナー名(3) × 自チーム(8) = 24通り。敵チームは画面に出ないため使えない

- **FullDB**: 全2^32状態が対象。状態sから7バトル分のコード c0..c6 を計算し、
  ファイル `{c5+c6*24}.bin` に `seedKey = c4 + c3·24 + c2·24² + c1·24³ + c0·24⁴` の昇順で
  **seed(u32 LE)のみ**を格納。約16GiB
- **LightDB**: 8回観測して先頭1回を捨てる方式。1バトル適用後に到達可能な状態(像)のみが対象。
  像の状態vから7バトル分 c1..c7 を計算し、ファイル `{c1+c2*24}.bin` に
  `seedKey = c3 + c4·24 + c5·24² + c6·24³ + c7·24⁴` の昇順で
  **(seedKey u32, 生成後seed u32)(LE)** を格納。値は「7バトル生成後の状態」であり
  検索結果をそのまま現在seedとして使える。約1GiB
- 像は全状態の**約2.98%**(現物と一致)。理論: 末尾採用PIDの条件(性格×性別の和 = 705/6400 ≈ 0.110)
  が上界で、逆向きパースの完走率(臨界分岐過程、実測約0.27)が掛かる

## 検証状態(重要)

済:
- バトル生成コア: `codb-gen selftest 256` と `verify-cs/`(C#ハーネス、PokemonCoRNGLibrary の `GenerateCode` を呼ぶ)の出力が**256件完全一致**
- スモーク(`--limit 33554432`): FullDBエントリ数=処理状態数、像の確保数が理論値 M(1−e^(−n/M))(M=1.28×10⁸)と一致

未:
- スモーク出力LightDBへの `verify --pairs`(ソート検査)
- 既知seedの観測列で、実物の`FullDBSearcher` / `LightDBSearcher`(PokemonCOSeedDataBaseAPI)がスモークDBから正しいseedを引けることの確認

未検証部分の残リスクはキーのパッキング順序・観測アラインメントの解釈違い。
壊れ方は「全く検索に当たらない」型なので、テスト2を1回通せば決着する。**本番生成(数時間)の前に必ずテスト2を通すこと。**

## 生成後にやること

1. `codb-gen verify <DIR>\LightDB --pairs` / `verify <DIR>\FullDB`
2. LightDBのエントリ数が **128,095,165** と一致するか確認(現物と同一集合になるはず。不一致なら生成ロジック差分を疑う。ただし現物は色回避や性別比の扱いが不明な旧ライブラリ製の可能性があるので、軽微な差は「現行ライブラリが正」)
3. 新旧Searcherでのクエリ突き合わせ
4. その後の発展としてLightDB圧縮フォーマット(設計書参照)の実装に進む場合、本ツールのソートフェーズ出力を差し替えるのが最短
